# Allure framework configuration
# Tells the Allure TestNG agent where to write result files.
# This value is also set via systemPropertyVariables in pom.xml (Maven takes precedence).
allure.results.directory=target/allure-results

