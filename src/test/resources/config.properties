# =============================================================================
#  Master API Test Automation Framework — Runtime Configuration
# =============================================================================
#
#  These values are the defaults for local development.
#  CI/CD pipelines should override sensitive values via environment variables
#  or -D system properties passed to Maven (e.g. mvn test -Dbase.uri.reqres=https://...).
#
#  Resolution order (highest to lowest precedence):
#    1. System property   (-Dkey=value on Maven command line)
#    2. Environment var   (KEY_NAME with dots replaced by underscores, uppercased)
#    3. This file
# =============================================================================

# JSONPlaceholder: free, stable, zero-auth REST API (replaces reqres.in which added paid auth)
base.uri.jsonplaceholder=https://jsonplaceholder.typicode.com
base.uri.restfulbooker=https://restful-booker.herokuapp.com
base.uri.soap=https://www.dataaccess.com/webservicesserver

connection.timeout.ms=10000
socket.timeout.ms=30000

default.user.page=2
default.user.id=2

#Restful Booker Demo Credentials
# Public documentation credentials. Override via environment variables or -D properties if needed.
restfulbooker.username=admin
restfulbooker.password=password123

